patterns:
  - "docs/**/*.md"

exclude:
  - ".worktrees/**"

on_missing_frontmatter: warn

defaults:
  strategy: code_changes
  watch:
    - "cmd/**/*.go"
    - "internal/**/*.go"
  ignore:
    - "**/*_test.go"
